{
  "_doc_substantial_paths": "Globs matched against changed file paths (relative to repo root). Any match triggers the audit-presence check.",
  "substantial_paths": [
    "editorial/drafts/**",
    "editorial/published/**",
    "src/pages/**",
    "src/components/charts/**",
    "src/components/home/**",
    "src/components/section/**",
    "src/layouts/**",
    "pipeline/blurbs/**"
  ],
  "_doc_substantial_extensions_in_pipeline": "FUTURE: Python files under pipeline/ matching model_filename_patterns will also be treated as substantial. Not enforced in v1.",
  "substantial_extensions_in_pipeline": [".py"],
  "_doc_model_filename_patterns": "FUTURE: Filenames matching these patterns under pipeline/ will be treated as substantial. Not enforced in v1.",
  "model_filename_patterns": ["*model*.py", "*model*.mjs"]
}
