[pytest]
testpaths = pipeline/tests
python_files = test_*.py
addopts = -ra --strict-markers
filterwarnings =
    # pytest-httpx emits a DeprecationWarning during plugin teardown on Python 3.14
    # related to event-loop scope; harmless for unit tests, so silence.
    ignore::DeprecationWarning
