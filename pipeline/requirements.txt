# Macro Research Department - Python pipeline dependencies
#
# Floors chosen for Python 3.11+ (project floor; see ARCHITECTURE.md ADR-0003).
# Versions pinned with >= rather than == so security updates flow; tighten to
# == once we have a lockfile and CI matrix.

httpx>=0.27,<0.29          # HTTP client w/ explicit timeouts + connection pooling; preferred over requests
tenacity>=8.2,<10           # retry / backoff decorators; replaces hand-rolled retry loops
pydantic>=2.7,<3            # boundary validation of upstream JSON shapes (StatCan, Valet, FRED)
pandas>=2.2,<3              # tabular wrangling; CSV IO; resampling
python-dateutil>=2.9,<3     # robust date parsing (Valet's "d" strings, BIS time periods, etc.)
lxml>=5.0,<7                # HTML table parsing for DoF Fiscal Monitor scraper
openpyxl>=3.1,<4            # XLSX reader for CREA MLS HPI bulk download
beautifulsoup4>=4.12,<5     # used alongside lxml for resilient HTML cleanup in Fiscal Monitor scraper
ruamel.yaml>=0.18,<0.20     # round-trip YAML for cycle-artifact front-matter (preserves comments/order; PyYAML lossy)

# Testing
pytest>=8.0,<9
pytest-httpx>=0.30,<0.36    # mocks httpx clients in tests; keeps tests off the live APIs
