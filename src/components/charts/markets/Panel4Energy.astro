---
/*
 * Panel4Energy - WTI and Brent.
 *
 * Pipeline emits `wti` (primary, USD/barrel) and `brent` (secondary).
 * Both USD/barrel; rendered as overlay.
 *
 * Synthetic-data fallback removed.
 */

import type { PanelData } from "../_shared/panelData";
import PanelLiveChart from "../_shared/PanelLiveChart.astro";
import PanelEmpty from "../_shared/PanelEmpty.astro";

interface Props {
  data?: PanelData | null;
}

const { data = null } = Astro.props;
---
{data && data.primary ? (
  <PanelLiveChart
    primary={data.primary}
    secondary={data.secondary}
    primaryLabel="WTI"
    secondaryLabel="Brent"
    dataPanelAttr="markets-4"
    ariaLabel="WTI and Brent oil prices, USD per barrel."
  />
) : (
  <PanelEmpty reason="Pipeline did not emit panel-4 data." />
)}
