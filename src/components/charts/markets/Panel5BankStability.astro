---
/*
 * Panel5BankStability - bank stability metrics.
 *
 * Editorial argument is Big-Six PCL / CET1 / uninsured residential.
 * Pipeline emits `boc_settlement_balances` only, which does not carry
 * the bank-stability argument; we fall back to PanelEmpty.
 *
 * Synthetic-data fallback removed.
 */

import type { PanelData } from "../_shared/panelData";
import PanelEmpty from "../_shared/PanelEmpty.astro";

interface Props {
  data?: PanelData | null;
}

const { data: _data = null } = Astro.props;
void _data;
---
<PanelEmpty reason="Big-Six bank-stability series not yet emitted." />
