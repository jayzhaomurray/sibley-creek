---
/*
 * Panel6FDIBySector - FDI by sector.
 *
 * Pipeline does not emit FDI by industry; PanelEmpty until the
 * series lands.
 *
 * Synthetic-data fallback removed.
 */

import type { PanelData } from "../_shared/panelData";
import PanelEmpty from "../_shared/PanelEmpty.astro";

interface Props {
  data?: PanelData | null;
}

const { data: _data = null } = Astro.props;
void _data;
---
<PanelEmpty reason="FDI by sector not yet emitted." />
