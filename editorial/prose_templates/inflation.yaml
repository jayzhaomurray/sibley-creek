section: inflation

slots:
  headline: cpi_all_items_nsa_yoy
  trim: cpi_trim
  median: cpi_median
  breadth: cpi_breadth_above3

surfaces:
  tileline:
    required: true
    variants:
      - when: 'true'
        text: "Headline CPI {latest(headline)|pct1}; trim {latest(trim)|pct1}, median {latest(median)|pct1}; {latest(breadth)|pct1} of the basket above 3%."
