section: output

slots:
  gdp: gdp_monthly
  services: gdp_industry_services
  goods: gdp_industry_goods
  gap: output_gap_mpr

surfaces:
  tileline:
    required: true
    variants:
      - when: 'true'
        text: "Real GDP {latest(gdp)|two_dec}T; services {latest(services)|two_dec}T, goods {latest(goods)|two_dec}T; output gap {latest(gap)|pct1}."
