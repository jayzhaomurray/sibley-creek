section: housing

slots:
  hpi: crea_hpi_canada_yoy
  starts: housing_starts
  arrears: cba_mortgage_arrears_national
  affordability: housing_affordability

surfaces:
  tileline:
    required: true
    variants:
      - when: 'true'
        text: "National HPI {latest(hpi)|pct1}; housing starts {latest(starts)|int}k SAAR; arrears {latest(arrears)|pct2}; affordability {latest(affordability) * 100|pct1}."
