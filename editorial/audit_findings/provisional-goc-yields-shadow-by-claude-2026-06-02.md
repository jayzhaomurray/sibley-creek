---
target: provisional GoC yield overlay shadow-mode diff
leader: codex
auditor: claude
task: Audit provisional GoC yield overlay shadow mode: BoC remains canonical history, scraped Trading Economics latest yields are quarantined, shadow bundles write under data/site_shadow, and live panel/site builders only accept optional in-memory overlays.
date: 2026-06-02
note: audit-diff hit Windows ENAMETOOLONG when passing the prompt as argv; this equivalent prompt was sent to Claude via stdin.
---
Verified against the actual repo. The quarantine architecture holds up — but I found one real code defect plus a couple of footguns. Findings first.  ---  ## Audit: provisional GoC yield overlay (shadow mode)  **Verdict: architecture is sound for shadow scope.** The core safety goals are met. One IMPORTANT correctness bug in the validator that defeats graceful partial-curve handling; the rest are MINOR/forward-looking.  ### Verification of the stated safety guarantees (all PASS) - **Live path never overlaid.** The only live callers — `pipeline/build.py:2992` and `pipeline/build_financial.py:437` — call `build_site_data(DATA_ROOT)` / `build_all_panel_data(DATA_ROOT)` with no `series_overlays`. Default is `None`; behavior unchanged. Only `shadow_goc_yields.py:164-165` passes overlays. - **No raw contamination.** `goc_yields.py` only *reads* `data/raw/yield_*.csv` (`_read_latest_official`); all writes target `data/provisional/` or `data/site_shadow/`. `apply_series_overlay` appends to an in-memory `df.copy()` only. - **Quarantine isolated from build + deploy.** Every gate (`check_panel_data_*`, `check_orphan_panels`, integrity) and every Astro import is hardcoded to `data/site/panel_data/` — never `data/site_shadow/`. Both `data/provisional/` and `data/site_shadow/` are gitignored (`.gitignore:48-49`) and currently untracked (`git ls-files` returns nothing). - **Complete-curve / same-date / range / move guards present** and unit-tested.  ---  ### BLOCKER None.  ### IMPORTANT  **1. `validate_payload` crashes (KeyError) on a partial or non-numeric curve when `data_root` is set — `goc_yields.py:214`.** The move-check loop runs `provisional_value = float(checked["values"][series])` for **every** required series. But a series that was missing from `observations` (line 169 `continue`) or had a non-numeric/non-finite value (lines 181/184 `continue`) was never written into `checked["values"]` (line 188). If the *remaining* maturities share one date, `as_of` is set, and with `data_root` passed the loop reaches the missing series and raises `KeyError`.  This is exactly the partial-curve case the system is meant to reject *gracefully*. Both real entry points pass `data_root` (`shadow_goc_yields.build_shadow` → `validate_payload(..., data_root=data_root)`, and `goc_yields.main`), so a live partial scrape crashes with an unhandled traceback instead of returning `status="invalid"` with the "missing required maturities" violation. `write_payload` never runs, so no diagnostic artifact is left, and `--allow-invalid` can't save it (the exception precedes the status check). It fails *closed* (no shadow built), but via crash, not the designed path — and the existing test masks it because `test_validate_payload_rejects_missing_maturity` calls `validate_payload` **without** `data_root`.  Fix: guard the move check, e.g. ```python if series not in checked.get("values", {}):     continue  # missing/non-numeric already recorded a violation above provisional_value = float(checked["values"][series]) ``` And add a regression test that passes `data_root` with a missing maturity.  **2. Live wiring will need a reader-visible provisional badge — not yet present (forward-looking).** The payloads carry `provisionalOverlay`/`provisionalOverlays` provenance, but no frontend renders a "provisional / scraped" indicator. Correct for *shadow* (nothing deployed). Flagging so the live-wiring gate doesn't ship scraped yields indistinguishable from official BoC values to readers. The plan's "Claude audit target → reader-visible ambiguity" item should block live flip until a badge exists.  ### MINOR  **3. `data_root=None` silently disables the "not newer than official" + move guards — `goc_yields.py:202`.** A complete same-date curve that is *older than or equal to* the official BoC row returns `status="ok"` when `data_root` is omitted. In practice `apply_series_overlay`'s own newer-than check (`overlays.py`) degrades it to a no-op, and both real callers pass `data_root` — so no live exposure. But the `ok` status is misleading. Consider requiring `data_root` (or recording a violation/warning when it's absent) so `ok` always means "checked against canonical."  **4. Hardcoded provenance path string — `panel_data.py:1266`.** `_collect_source_files` appends the literal `"data/provisional/goc_yields_latest.json"` rather than deriving from `goc_yields.DEFAULT_OUTPUT`. Drifts silently if the artifact path moves. Reference the constant.  **5. Parser is layout-fragile by design — `goc_yields.py:_CURVE_LINE_RE`.** The `.*?` between value and the `Mon/DD` date will bind to the first date-like token in the row; a TE column reorder could capture a percentage-change column as `value`. Range (0–25) and 0.75pp move checks catch gross misreads, but an in-range misread would pass. Acceptable for shadow + provenance-stamped output; worth a stricter column assertion before live. The empty-match case fails closed correctly (→ missing maturities → invalid).  **6. Overlay appends unconditionally to an empty canonical frame — `overlays.py`.** The newer-than guard is skipped when `out.empty` or no `date` column. Harmless for yields (always have history); noting because the "never older than canonical" guarantee at the overlay layer is conditional on canonical data existing.  ---  **Bottom line:** Ship-safe as shadow. Fix #1 before any further reliance on the validator (it's the one place the partial-curve guarantee actually breaks), wire #3/#4 opportunistically, and treat #2 + #5 as hard gates on the eventual live flip.  Want me to dispatch the #1 fix (one-line guard + regression test) to backend-engineer now, or leave it for Codex to address?
